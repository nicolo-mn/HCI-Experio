# Experio

Per runnare app:

Prima volta (setup):
`cd A5/my-chat-app`
`npm install`

Eseguire:
Cambia template target in App.vue (da `<Chat />` a qualcos'altro)
`npm run dev`
Vai a `http://localhost:5173`